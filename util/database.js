const Sequelize = require('sequelize');
const sequelize = new Sequelize();







//utiliser pour un BD SQL
/*const  mysql = require('mysql2');

const pool = mysql.createPool({
   host: 'localhost',
    user: 'root',
    database: 'node-complete',
    password: 'admin'
});

module.exports = pool.promise();*/
